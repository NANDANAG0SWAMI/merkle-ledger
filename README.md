# merkle-ledger
**Merkle Ledger** is a cryptographic audit logging microservice built on Hedera Consensus Service. Every message submitted is hashed, batched into a Merkle tree, and anchored immutably on-chain. Anyone can verify a log entry existed at a specific time — without trusting the server, without reading the full history.
